<ion-view title="Cardápio da Semana" id="page5" class=" " ng-init="metodoInicial('cardapio^daianeEventosCtrl')">
    <ion-nav-bar>
        <ion-nav-back-button style="color: #ffffff" class="button-icon icon-left ion-ios-arrow-left">
        </ion-nav-back-button>
    </ion-nav-bar>
    <ion-content class="has-header">
        <div ng-repeat="cardapio in fworkEscopo.tabCardapio">
        <div class="item item-divider">
            <h2 class=" ">{{cardapio.diaSemana}}</h2>
        </div>
	       <div class="item item-text-wrap">
               <p ng-repeat='item in cardapio.itens'>{{item.texto}}</p>
	       </div>
	   </div>
    </ion-content>
</ion-view>